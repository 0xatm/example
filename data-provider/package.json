{
  "name": "data-provider",
  "version": "1.0.0",
  "description": "A data provider mocked up server",
  "main": "build/index.js",
  "license": "MIT",
  "typings": "./build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "yarn clean && tsc && node build/index.js",
    "clean": "rimraf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ethers": "^5.0.31"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  }
}
